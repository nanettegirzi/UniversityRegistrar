@{
  Layout="_Layout";
}

@if (Model.Count != 0)
{
<ul>
@foreach (var course in Model)
{
    <li><a href="/courses/@course.GetId()">@course.GetCourseName()</a>      <a href="/course/@course.GetId()/update">Edit</a> | <a href="/course/@course.GetId()/delete">Delete</a></li>
      }
    </ul>
  }
  <h4><a href="/course/new">Add a new course</a></h4>
  <h4><a href="/">Return home</a></h4>
  </div>

}
